<%
  @page_title = :add_form.l
  @tabbed_navigation_items = project_tabbed_navigation(:forms)
  @bread_crumbs = project_crumbs(:add_form, [{:title => :forms, :url => "/project/#{@active_project.id}/form/index"}])
  
  @page_actions = []

  @additional_stylesheets = ['project/forms']
%>

<%= form_tag :controller => 'form', :action => 'add' %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= :add_form.l %></button>
</form>
