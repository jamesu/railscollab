<%
  @page_title = :edit_form.l
  @tabbed_navigation_items = project_tabbed_navigation(:forms)
  @bread_crumbs = project_crumbs(:edit_form, [{:title => :forms, :url => "/project/#{@active_project.id}/form/index"}])
  
  @page_actions = []

  @additional_stylesheets = ['project/forms']
%>

<%= form_tag :controller => 'form', :action => 'edit', :id => @form.id %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= :edit_form.l %></button>
</form>
