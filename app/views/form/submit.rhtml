<%
  @page_title = @form.name
  @tabbed_navigation_items = project_tabbed_navigation(:overview)
  @bread_crumbs = project_crumbs(@form.name)
%>
<%= form_tag :controller => 'form', :action => 'submit', :id => @form.id %>
<%= error_messages_for :form %>
  
<% if !@form.description.nil? %>
  <div class="formDescription"><%= textilize @form.description %></div>
<% end %>
<div>
	<%= text_area_tag 'form[content]', '', :class => 'editor', :rows => 10, :cols => 40 %>
</div>
    <button class="submit" type="submit">Submit</button>
</form>
