= form_with(model: @commented_object, url: object_comments_url(@commented_object), method: :post, multipart: true) do |form|
  = render partial: 'form', locals: {form: form}
  %button.submit{type: "submit"}= t('add_comment')
