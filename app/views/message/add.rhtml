<%
  @page_title = 'Add message'
  
  @tabbed_navigation_items = project_tabbed_navigation(1)
  @bread_crumbs = project_crumbs("Add message", [{:title => 'Messages', :url => "/project/#{@active_project.id}/message"}])
  
  @page_actions = []
  
  @additional_stylesheets = ['project/messages']
%>
<!-- <script type="text/javascript" src="modules/addMessageForm.js"></script> -->

<%= form_tag( {:controller => 'message', :action => 'add'}, :multipart => true ) %>
<%= render :partial => 'form', :locals => {:is_new => true} %>
    <button class="submit" type="submit">Add message</button>
</form>
