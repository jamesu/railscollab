<%
  @page_title = :edit_project.l
  
  @tabbed_navigation_items = administration_tabbed_navigation(:projects)
  @bread_crumbs = administration_crumbs(:edit_project, [{:title => :projects, :url => "/administration/projects"}])
  
  @page_actions = []
  
  @additional_stylesheets = []
%>

<%= form_tag :controller => 'project', :action => 'edit' %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= :edit_project.l %></button>
</form>
