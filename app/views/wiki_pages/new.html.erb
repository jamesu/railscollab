<%= form_for(@wiki_page, :url => project_wiki_pages_path(@active_project)) do |form| %>
  <%= render :partial => form %>
  <div class="buttons">
    <button class="submit" type="submit"><%= t('add_page') %></button>
    <%= preview_button %>
  </div>
<% end %>
<div id="preview"></div>
