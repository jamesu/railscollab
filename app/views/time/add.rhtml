<%
  @page_title = :add_time.l
  @tabbed_navigation_items = project_tabbed_navigation(:time)
  @bread_crumbs = project_crumbs(:add_time, [{:title => :time, :url => "/project/#{@active_project.id}/time/index"}])
  
  @page_actions = []

  @additional_stylesheets = ['project/time']
%>

<%= form_tag :controller => 'time', :action => 'add' %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= :add_time.l %></button>
</form>
