<%

  @page_title = (h @time.name)
  @tabbed_navigation_items = project_tabbed_navigation(4)
  @bread_crumbs = project_crumbs((h @time.name), [{:title => 'Time', :url => "/project/#{@active_project.id}/time"}])
  
  @page_actions = []
  
  @additional_stylesheets = ['project/time']
%>
<div id="times">
<%= render :partial => 'time/view_time', :collection => [@time] %>
</div>
