<%= form_tag category_path(:id => @category.id), :method => :put, :data => { turbo_method: :put } %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= t('edit_category') %></button>
</form>
