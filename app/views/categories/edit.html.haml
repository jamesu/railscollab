= form_with(model: @category, url: project_category_path(@category.project, @category)) do |form|
  = render partial: 'form', locals: {form: form}
  %button.submit{type: "submit"}= t('edit_category')
