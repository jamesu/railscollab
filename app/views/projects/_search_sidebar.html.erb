<% if !@tag_names.empty? %>
<div class="sidebarBlock">
  <h2><%= t('tags') %></h2>
  <div class="blockContent">
    <p><%= t('tags_used_in_project') %>:</p>
    <ul class="listWithDetails">
<% is_public = @logged_user.member_of_owner? %>
    <% @tag_names.each do |tag| -%>
      <li><%= link_to h(tag.tag), project_tag_path(@active_project, CGI.escape(tag.tag)) %> <span class="desc"> - <%= t('tag_object_count', :count => Tag.count_by(tag.tag, @active_project, !@logged_user.member_of_owner?)) %></span></li>
    <% end %>
    </ul>
  </div>
</div>
<% end %>
