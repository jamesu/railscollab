<%
  @page_title = :edit_user.l
  @tabbed_navigation_items = administration_tabbed_navigation(:people)
  @bread_crumbs = administration_crumbs(:edit_user, [{:title => :people, :url => '/administration/people'}])
  
  @page_actions = []
  
  @additional_stylesheets = []
%>

<%= form_tag :controller => 'user', :action => 'edit', :id => @user.id %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= :edit_user.l %></button>
</form>
