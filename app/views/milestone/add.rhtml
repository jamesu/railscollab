<%
  @page_title = "Add milestone"
  @tabbed_navigation_items = project_tabbed_navigation(3)
  @bread_crumbs = project_crumbs('Add milestone', [{:title => 'Milestones', :url => "/project/#{@active_project.id}/milestone/add"}])
  
  @page_actions = []

  @additional_stylesheets = ['project/milestones']
%>

<%= start_form_tag :controller => 'milestone', :action => 'add' %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit">Add milestone</button>
</form>