= form_with(model: @task_list, url: project_task_list_path(@task_list.project, @task_list), method: :put, multipart: true) do |form|
  = render partial: 'form', locals: {form: form}
  %button.submit{type: "submit"}= t('edit_task_list')
