<%= form_tag project_task_list_path(@task_list.project, :id => @task_list.id), :data => { turbo_method: :put } %>
<%= render :partial => 'form' %>
    <button class="submit" type="submit"><%= t('edit_task_list') %></button>
</form>
